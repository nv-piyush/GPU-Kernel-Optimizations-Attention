# Python environment for Colab analysis notebooks
# Runtime: Google Colab (CPU runtime is sufficient — no GPU needed for analysis)
# Python version: 3.10.x (default Colab as of early 2026)
#
# These are pre-installed in Colab. Listed here for reproducibility.
# To install locally: pip install -r requirements.txt

torch==2.6.0
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.1
tabulate==0.9.0       # for .to_markdown() in analysis notebook